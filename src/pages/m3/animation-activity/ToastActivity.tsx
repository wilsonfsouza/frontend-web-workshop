// =============================================================================
// Animation Activity — Starter File
//
// Build a success toast notification that slides up when it appears.
//
// Requirements:
// 1. A green circle with a ✓ checkmark on the left
// 2. A title ("Changes saved") and description next to it
// 3. A "Dismiss" button on the right side
// 4. The whole toast should slide up using animate-slide-up
//
// Available animations (defined in index.css via @theme):
//   animate-fade-in, animate-slide-up, animate-bounce-in,
//   animate-pulse-slow, animate-wiggle
//
// Tip: look at the NotificationCard exercise for layout inspiration!
// =============================================================================

export function ToastActivity() {
  return (
    // Replace this with your toast notification
    <div>
      <p className="text-sm text-gray-400">Your toast goes here...</p>
    </div>
  );
}
